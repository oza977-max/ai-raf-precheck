---
schema_version: 1
---

# Exploratory Session — explore-004

## Charter

```yaml
schema_version: 1
session_id: explore-004
mission: "Correction session for charter 003. Three objectives, in the shape charter 002 used. First, re-test D-002 against reality: 003 recorded that the guided form has no jurisdiction field, which is false — the field exists as a checkbox fieldset and was missed because the form was filled by script and the page text was read truncated. Establish what the real defect is, if any, and record it honestly rather than quietly dropping the wrong one. Second, re-check D-001, D-003 and D-007, all observed through the same faulty instrument, by driving the form by hand — clicking and typing, not setting values through JavaScript — since a script-set value bypasses exactly the React state paths those three findings describe. Third, re-confirm D-004, D-005, D-006 and D-008 still reproduce, and record whether they do rather than carrying them forward on trust. Charter 003 stays as the record of what was found and what was got wrong; this charter supersedes it as the gate."
timebox_minutes: 60
tour: feature
runner: unassigned
```

**Target:**

- http://localhost:5173
- src/components/StructuredForm.tsx
- src/components/IntakeFlow.tsx
- src/components/RegisterView.tsx
- src/components/RegisterDetail.tsx
- src/seeds/sample-register.ts

## Session Log

- Cleared localStorage and every IndexedDB database before starting, so this is a genuine first-run state -- the step charter 003 omitted.
- Instrument note: the first browser tab would not dispatch synthetic clicks at all (clicks landed on the correct element by hit-test but never fired a handler). Opened a second tab, where clicks worked. Button activation stayed intermittently unreliable, so some navigation was performed programmatically; every FINDING below was established by real keystrokes or by DOM query, not by scripted state.
- Typed a 135-character description by keyboard, including 'Operates in the UK and EU'. Clicked 'Read & extract ->'.
- Duplicate check reported 'checked 1 register entry' -- correct, and correctly singular. 003's D-008 does not reproduce; withdrawn (O-001).
- Guided form rendered with sf-name and sf-description both empty. D-001 reproduces.
- Enumerated the form by DOM query rather than by reading rendered text: 14 selects AND 7 checkboxes, six of them sf-jurisdiction-UK/US/EU/CA/SG/JP. The jurisdiction field EXISTS. Charter 003's D-002 was wrong -- it was missed because the earlier walk enumerated only <select> elements and read a page dump truncated above the fieldset.
- Queried the form for required markers: zero elements carry required / aria-required / .required, and the word 'required' appears nowhere. D-003 reproduces.
- Filled name and description by keyboard and set the twelve required selects, deliberately ticking NO jurisdiction. Continue enabled. This is the real D-002.
- Graph review screen showed 'vendor: internal' though no vendor was chosen. D-007 (now D-006) reproduces.
- Confirm screen read 'JURISDICTIONS / None specified' directly above 'By confirming, you attest the data-flow graph above is accurate'. No warning.
- Evaluated. Verdict: Approved with controls, 13 of 18 invariants triggered -- and no REGULATORY REASONING CHAIN panel, no pack reference anywhere. A complete verdict with no regulatory basis, produced silently.
- Loaded the sample use cases into the cleared register. All six stamped policy 1.3, evaluated today, no Stale flag anywhere -- the seeds are generated by a live evaluate() run. 003's D-005 does not reproduce; withdrawn (O-002).
- Register row for the just-completed pre-check read 'Mortgage servicing assistant -- input'. D-004 reproduces. Sample seeds carry the correct name, so the defect is specific to form-built graphs.
- Switched to the 2LoD role and opened '[SAMPLE] Client-facing wealth chatbot' (High tier, awaiting sign-off). Whole page is 779 characters: status, notes box, Approve / Request correction, audit trail. No control set, no citations, no margin. D-006 reproduces.
- Console clean throughout.

## Defects

### D-001: the form's name and description fields are both empty strings. Confirmed by hand

**Severity:** Critical
**Tour:** feature
**Given:** a keyless user on the New pre-check screen who has written a plain-language description
**When:** they click 'Read & extract ->' and are routed to the guided intake form
**Then:** the form's name and description fields are both empty strings. Confirmed by hand this session: 135 characters were typed with real keystrokes, including the sentence 'Operates in the UK and EU', and `sf-name` and `sf-description` both read length 0 on arrival. The jurisdiction sentence is discarded along with the rest, which matters because the form's jurisdiction checkboxes are the thing that would have used it.
**Reproduction:** 1. Clear site storage so the app is in a genuine first-run state. 2. Type any description into the Describe box using the keyboard. 3. Click 'Read & extract ->', then 'This is a new use case ->'. 4. Read `sf-name` and `sf-description`: both empty.
**Stub-path:** 

### D-002: 'Continue' enables with zero jurisdictions selected, and the flow runs to a full

**Severity:** Critical
**Tour:** feature
**Given:** a user completing the guided intake form who does not tick any of the six jurisdiction checkboxes (UK, US, EU, CA, SG, JP), none of which is ticked by default
**When:** they fill every other required field and look for the way forward
**Then:** 'Continue' enables with zero jurisdictions selected, and the flow runs to a full verdict. The Confirm screen reports 'JURISDICTIONS -- None specified' and asks the user to attest that the graph is accurate; the verdict then renders with no REGULATORY REASONING CHAIN panel and no pack mention at all. Nothing at any point says a jurisdiction is needed or that the citations are missing because of its absence. The completeness predicate tests the jurisdictions array for presence only, and an empty array is truthy. This finding supersedes charter 003's D-002, which claimed the form had no jurisdiction field. It does -- 003 was wrong. The defect is the missing guard, not a missing feature, and the user-facing consequence is the same: a verdict with no regulatory basis, produced silently.
**Reproduction:** 1. Open the guided form. 2. Fill name, description and all twelve required selects; tick NO jurisdiction checkbox. 3. Observe Continue is enabled. 4. Continue through Graph and Confirm -- Confirm reads 'JURISDICTIONS: None specified'. 5. Evaluate: verdict renders 'Approved with controls' with no regulatory chain and no packs.
**Stub-path:** 

### D-003: nothing on the form marks any field as required. Confirmed by query this session

**Severity:** Important
**Tour:** feature
**Given:** the guided intake form, on any partially completed state
**When:** the user looks for what is still required in order to enable Continue
**Then:** nothing on the form marks any field as required. Confirmed by query this session: zero elements carry `required`, `aria-required` or a `.required` class, and the word 'required' does not appear in the form's text at all. Continue is disabled until the last of thirteen fields is set, with no message naming what is outstanding.
**Reproduction:** 1. Open the guided form and fill some but not all fields. 2. Observe Continue stays disabled with no accompanying message. 3. Query the form for required markers: zero matches.
**Stub-path:** 

### D-004: the row is titled 'Mortgage servicing assistant -- input' -- the input node's la

**Severity:** Important
**Tour:** feature
**Given:** a completed pre-check submitted under the use case name 'Mortgage servicing assistant'
**When:** the user opens the Register
**Then:** the row is titled 'Mortgage servicing assistant -- input' -- the input node's label rather than the use case name. Re-confirmed by hand this session against a freshly seeded register. Note the sample seeds use the correct name, so the defect is specific to graphs built by the intake form.
**Reproduction:** 1. Complete a pre-check with a distinct name. 2. Open the Register: the Use Case Name column shows the name with ' -- input' appended.
**Stub-path:** 

### D-005: the detail page carries tier, track, status, a notes box, Approve and Request-co

**Severity:** Critical
**Tour:** feature
**Given:** a 2LoD reviewer opening a High-tier use case awaiting their sign-off
**When:** they open it from the Register to decide whether to approve it
**Then:** the detail page carries tier, track, status, a notes box, Approve and Request-correction buttons, and the append-only audit trail -- and no verdict reasoning. Re-confirmed by hand this session: the entire page is 779 characters and contains no control set, no regulatory citation, and no governance margin. The only invariant anywhere on it is the binding one, which appears incidentally inside a one-line audit summary. The reviewer is asked to attest to a decision whose basis the page does not show.
**Reproduction:** 1. Load the sample use cases and switch to the 2LoD role. 2. Open '[SAMPLE] Client-facing wealth chatbot' from the Register. 3. Read the whole page: no invariant list, no control set, no citations, no margin.
**Stub-path:** 

### D-006: the node reads 'vendor: internal' -- a value the user never chose. Re-confirmed 

**Severity:** Important
**Tour:** feature
**Given:** the guided form's optional vendor field, left untouched
**When:** the Review extracted graph screen renders the processing node
**Then:** the node reads 'vendor: internal' -- a value the user never chose. Re-confirmed by hand this session. The next screen asks the user to attest the graph is accurate to the best of their knowledge, so a silently defaulted value is being folded into an attestation.
**Reproduction:** 1. Complete the guided form without selecting a vendor. 2. Click Continue. 3. The PROCESSING node shows 'vendor: internal'.
**Stub-path:** 


## Observations

### O-001: 003's D-008 ('checked 7 register entries' before demo data was loaded) DOES NOT 

**Tour:** feature
**Given:** a browser profile in which an earlier session's data was still persisted in IndexedDB
**When:** charter 003 ran the duplicate check and the register without clearing site storage first
**Then:** 003's D-008 ('checked 7 register entries' before demo data was loaded) DOES NOT REPRODUCE. Against genuinely cleared storage the step reads 'checked 1 register entry' -- correct, being the seeded AIGate self-assessment -- and the singular/plural form is right. The count was accurate; the browser was not empty. Withdrawn.
**Stub-path:** 

### O-002: 003's D-005 ('five of six samples flagged Stale at policy v1.1') DOES NOT REPROD

**Tour:** feature
**Given:** the same stale-storage condition
**When:** charter 003 loaded the sample use cases and read the Register
**Then:** 003's D-005 ('five of six samples flagged Stale at policy v1.1') DOES NOT REPRODUCE. Against cleared storage all six samples are stamped policy 1.3 -- the current version -- with no Stale flag anywhere, because the seeds are generated by a live evaluate() run at seed time rather than carrying hardcoded versions. The v1.1 rows in 003 were seeded by an earlier session and persisted. Withdrawn.
**Stub-path:** 

### O-003: three of 003's eight findings were instrument artefacts rather than product defe

**Tour:** feature
**Given:** the exploratory session itself, as an instrument
**When:** charter 003 drove the guided form by setting select values through JavaScript and read the page through a truncated text dump
**Then:** three of 003's eight findings were instrument artefacts rather than product defects (D-002 wrong, D-005 and D-008 withdrawn). The common causes: a script-set value bypasses the React state path the finding claims to describe; a truncated page read hides fields below the cut, which is how a visible six-checkbox jurisdiction fieldset was reported as absent; and an uncleared browser carries a previous session's register into what is assumed to be a first run. Standing lesson for future charters: clear site storage before a first-time-user tour, drive inputs with real keystrokes, and enumerate form controls by DOM query rather than by reading rendered text.
**Stub-path:** 


## Overall Assessment

This charter exists because charter 003 got things wrong, so the first duty is to say plainly what it got wrong. Three of its eight findings were artefacts of how it was driven rather than defects in the product. D-002 claimed the guided form has no jurisdiction field; it has six checkboxes, missed because the earlier walk enumerated only select elements and read a page dump that was truncated above them. D-005 and D-008 both rested on a browser carrying a previous session's data into what was assumed to be a first run; against cleared storage the demo seeds stamp the current policy version and the duplicate check counts correctly. Both are withdrawn. What survives is five defects re-confirmed by hand and one re-filed. The re-filed one matters most: the form does ask for jurisdiction, but nothing requires an answer, so a user can tick nothing, pass a completeness check that treats an empty array as filled, attest to a graph reading 'None specified', and receive a full verdict with no regulatory chain and no packs -- with nothing anywhere telling them the citations are missing or why. The consequence is what charter 003 described even though its cause was wrong. Alongside it, the product still discards the description it asks the user to write, and still asks a 2LoD reviewer to sign off on a page that does not show them the verdict. Those two are the same defect in different places -- reasoning the product produced, not shown to the person who needs it. The standing lesson is recorded as O-003: clear storage before a first-time-user tour, drive inputs with real keystrokes, and enumerate controls by DOM query rather than by reading rendered text. Every finding above was established that way.
