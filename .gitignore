.DS_Store
node_modules/
dist/
.env
*.log
*.local
*.tsbuildinfo

# NOTE: build/ is intentionally NOT ignored — it holds GVM's build/prompts/
# and build/handovers/, the chunk-by-chunk persistence mechanism. Vite's
# compiled output goes to dist/ (ignored above), not build/.
Mockup prototype request/

# Commercial strategy + internal adoption plan — deliberately not published.
# Purged from git history 2026-07-26; keep it local.
design-vision.md
