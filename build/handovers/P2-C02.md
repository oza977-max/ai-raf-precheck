# Handover: P2-C02 — IndexedDB stores (audit + register)

## Status: Complete
## Commit: d1883cd (merged into main at fa72d33)
## Branch: main (built on worktree-agent-ac987f0a21bd9652e, merged and deleted)

## Files Created

None — this chunk only modified existing P1-C01 files (widening/replacing their contents), no
new files.

## Files Modified

- `src/store/types.ts` — widened `AuditEvent`/`AuditEventType`/`AuditEventPayload` from the P1-C01
  single-variant stub to the full 9-variant union per `verdict-audit.md §4.3`; added register types
  (`RegisterNode`, `RegisterNodeMetadata`, `RegisterEdge`, `LifecycleStage`, `UseCaseSummary`);
  deleted the old `RegisterRow` stub type
- `src/store/db.ts` — added `openRegisterDb()` for the `aigate-register` database (`register_nodes`
  + `register_edges` object stores, 4 indexes total); `openAuditDb()` untouched
- `src/store/audit.ts` — added `getAllForExport()` (full unindexed export, RG-4/RG-5)
- `src/store/register.ts` — **full replacement** of the P1-C01 in-memory stub
  (`addUseCase`/`getAllUseCases`) with the real `RegisterStore` implementation: `addNode`,
  `addEdge`, `updateUseCaseVerdictSummary`, `updateLifecycleStage`, `getUseCases`, `getUseCase`,
  `getGraph`, `getBlastRadius`, `exportAll`
- `src/store/db.test.ts` / `src/store/audit.test.ts` — updated P1-C01 fixtures using the now-invalid
  `event_type: 'skeleton_test'` literal to a valid `AuditEventType` (`'use_case_created'`) with a
  matching payload; added new tests for `openRegisterDb()` and `getAllForExport()`
- `src/components/IntakeFlow.tsx` — updated register call sites from the old stub interface
  (`addUseCase`, `getAllUseCases`) to the real `RegisterStore` interface (`addNode` constructing a
  `RegisterNode` with `node_type: 'use_case'`, `getUseCases('all')`); switched to an async
  `refreshRegister` pattern wired via `useEffect`

## Tests (verified)

- Count: **16 tests passing** (P2-C02's own commit state, before the P2-C01 merge)
- Verified exit code: 0
- Test command: `npm test`
- Parsed output: "Test Files 5 passed (5) / Tests 16 passed (16)" (at the pre-merge commit)

**Post-merge phase-completion count (both chunks combined, after conflict resolution):**
`npm test` → Test Files 7 passed (7) / Tests 24 passed (24), exit code 0. Verified directly via
Bash tool, not inferred — see the phase-completion section below.

Acceptance test Red commit / Green commit: `TDD-1 exempted: internal store layer — no new
user-facing component this chunk; the existing P1-C01 WalkingSkeleton.test.tsx acceptance test
was re-validated after the IntakeFlow.tsx upstream fix (register interface change) and confirmed
still passing, serving as this chunk's acceptance-level proof point per the build prompt's
TDD Approach section.`

## Test Command

`npm test` — full suite. `npm test -- src/store/register.test.ts` for the new register store
tests specifically.

## Hard Gate 8 (chunk-acceptance smoke — GATE-1)

Prompt file mtime is after `_V2_1_0_RELEASE_DATE` (2026-04-28) — gate applies, not exempt.

Smoke command: `npm test -- src/components/__tests__/WalkingSkeleton.test.tsx`

Exit code: 0

Structural assertions verified:
- The full intake flow (description → verdict → register row) still renders end-to-end after the
  register interface swap
- A submitted use case appears in the register table, now backed by real IndexedDB writes through
  `addNode()`/`getUseCases('all')` instead of the P1-C01 in-memory array
- `npm run build` completes with zero TypeScript errors (strict mode)
- The nested-keypath index (`by_submitted_by` on `metadata.submitted_by`) was verified with a real
  `fake-indexeddb` round-trip test in `db.test.ts` — confirms dot-path `createIndex` works under
  the project's `fake-indexeddb` version, not assumed

## Diff Summary

8 files changed, 585 insertions(+), 36 deletions(-)

Diff budget delta: 0

## Code Review Findings

**Review passes:** [(1, 0)]

Clean on first review pass — the independent review dispatched after the P2-C01+P2-C02 merge
(covering both chunks together) returned zero Critical/Important findings. Full detail:

- BC-P2C02-01 (`updateLifecycleStage` calls `audit.append()` with `lifecycle_stage_changed` in the
  same function body) — confirmed clean, verified by both grep and a dedicated round-trip test
- BC-P2C02-02 (role filtering via `by_submitted_by` index, not in-memory `.filter()` on a `by_type`
  scan) — confirmed clean
- BC-P2C02-03 (old `RegisterRow`/`addUseCase`/`getAllUseCases`/`__resetForTests` fully removed) —
  confirmed clean via grep, zero matches in `src/`
- `UseCaseSummary` computed at read time (not dual-written to a separate store) — confirmed
- Real `fake-indexeddb` round-trips throughout, mock budget = 1 respected (only the IndexedDB
  boundary itself is "mocked", via a real polyfill, not hand-written stubs) — confirmed
- Module boundaries (`src/store/*` no React/LLM imports) — confirmed clean

**Minor findings (informational, no fix required):**
- `updateLifecycleStage()`'s node-update and audit-append are two separate IndexedDB writes, not
  wrapped in a shared transaction — a partial-write risk if the process crashes between the two
  calls. This is explicitly documented as an accepted V1 limitation in both the spec
  (`register-lifecycle.md §6`) and the code's own comment — not a silent defect, confirmed the
  comment's honesty matches actual behavior.
- `db.put()` (not `db.add()`) is used for node/edge writes in `register.ts` — correct and
  intentional, since these are legitimate mutable-record updates, distinct from the audit log's
  append-only `db.add()` discipline (verified this distinction is deliberate, not an inconsistency).

## Deviations from Spec

1. **`getGraph()` and `updateUseCaseVerdictSummary()` were listed in the prompt's interface code
   block but not detailed in prose** — implemented straightforwardly for interface completeness
   (future chunks P3/P4 depend on them per the wiring matrix): `getGraph()` returns the use-case
   node plus its outgoing-edge neighbors; `updateUseCaseVerdictSummary()` patches `tier`/`track`
   onto the node's `metadata`. No spec conflict, just filling in an implementation detail the
   prompt left to the builder's judgment.

## Surfaced Requirements

None. No spec gaps discovered.

## Upstream Fixes

- `src/components/IntakeFlow.tsx` — register call sites updated from `addUseCase`/`getAllUseCases`
  (P1-C01's stub interface) to `addNode`/`getUseCases('all')` (the real `RegisterStore` interface).
  Wired via a `useEffect`-triggered `refreshRegister` async callback since the new calls return
  Promises. P1-C01's `WalkingSkeleton.test.tsx` acceptance test re-run and confirmed still passing.
- `src/store/db.test.ts` / `src/store/audit.test.ts` — P1-C01's test fixtures using the now-invalid
  `event_type: 'skeleton_test'` literal updated to `'use_case_created'` with a matching payload, so
  they typecheck against the widened `AuditEventType` union and continue to pass unmodified in
  behavior (still testing the same append/read round-trip, just with a valid event shape).

## Notes for Downstream Chunks

- **The `AuditEventPayload` variants for `verdict_produced`/`graph_corrected`/`verdict_corrected`
  currently type their nested fields as `unknown`** (`verdict: unknown`, `correction: unknown`) —
  this is intentional per the prompt (the real `Verdict`/`GraphCorrection` types land in P3-C01/
  P4-C01 respectively). Whichever chunk lands those types should also tighten these payload
  variants to use them instead of `unknown`.
- **`RegisterNodeMetadata`'s `use_case` variant types `tier`/`track` as `string | null`**, not the
  real `Tier`/`Track` union from `src/engine/types.ts` — a deliberate, minor looseness to avoid a
  premature cross-import before P3-C01's engine lands fully. A future chunk (likely P3-C01 or
  P6-C01) should tighten this once the engine types are stable and widely consumed.
- **The nested-keypath index pattern (`createIndex('by_submitted_by', 'metadata.submitted_by')`)
  is now proven to work** under this project's `fake-indexeddb` version — future chunks needing
  similar nested-field indexes can follow this pattern with confidence, it's not a guess anymore.

## Retrospective

**Harder than expected:** none of the deliverables were particularly hard — the framework
reference code samples in the prompt matched the actual `idb` API closely, and the existing
`openAuditDb()` pattern from P1-C01 was a clean template to mirror for `openRegisterDb()`.

**Easier than expected:** the `IntakeFlow.tsx` upstream fix (swapping the register interface) was
mechanically straightforward once the new `RegisterStore` functions existed — no design decisions
needed, just a signature swap plus an async/await adjustment.

**What I'd do differently:** nothing significant for this chunk. The one thing worth flagging for
future parallel-dispatch chunks: both P2-C01 and P2-C02 independently touched `IntakeFlow.tsx`
(expected, per the prompt's own "Upstream Fixes" instructions to both chunks), which produced a
predictable merge conflict on the import block. Future prompt-writing for parallel chunks that
both touch the same downstream consumer file should perhaps note the *other* chunk's expected
import addition explicitly, so the conflict resolution is even more mechanical than it already was.

---

*Developed using the Grounded Vibe Methodology*
