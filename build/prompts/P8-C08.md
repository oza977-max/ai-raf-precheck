# Build Prompt: P8-C08 — The sign-off names the verdict it attests to

## Chunk Summary

The `twoloD_reviewed` payload is `{ type, action, notes? }` — it carries no
reference to the verdict being signed off. R3-RD-3's fit criterion ("the audit
event written on sign-off refers to that same verdict") is unimplementable as
the schema stands, and TC-R3-RD-3-02 could only be written vacuously.

This chunk adds `verdict_id`, threads it **from the render**, and refuses the
write where the rendered verdict is no longer current.

**Closing chunk of Phase 8.** It also closes the trace-coverage gaps left by
P8-C07 (see Deliverables).

## Expert Panel

| Expert | Work | Role in This Chunk |
|---|---|---|
| Nancy Leveson | *Engineering a Safer World* | The race, and what an unrecoverable attestation costs |
| Kent Beck | *TDD by Example* | A test that could only pass vacuously is not a test |
| Michael Nygard | *Release It!* | Refuse the write rather than absorb the conflict |
| Fagan | Formal inspection | BC-001 |

## Git Strategy
Current branch (`main`).

## Dependencies

- **P8-C07** (pushed) — `RegisterDetail` renders the verdict; the rendered
  verdict is `latestVerdict`, derived from the exported
  `findLatestVerdictEvent`. C07's handover states: thread **that** id, do not
  re-derive at write time.

## Deliverables

- `src/store/types.ts` — `twoloD_reviewed` payload gains `verdict_id: string`.
- `src/components/RegisterDetail.tsx` — the id threaded from the render into
  both sign-off actions; the staleness refusal.
- `src/components/__tests__/RegisterDetail.test.tsx` — TC-R3-RD-3-01, -3-02,
  -3-03, plus the coverage gaps below.
- Existing fixtures constructing `twoloD_reviewed` payloads.

**Coverage gaps to close (left by P8-C07, found by cross-reading ids):**
- **TC-R3-RD-1-02** — invariant and control ids match the intake verdict as
  **sets**, not rendered text.
- **TC-R3-RD-2-02** — currently covered by a test whose name reads
  `TC-R3-RD-2-01/-02`, which does not resolve as a trace id. Split it.
- **TC-R3-RD-5-01** — policy-authored content rendered as text, never markup
  (`[SECURITY]`, OWASP; `verdict-audit.md` §13.2a is the standing invariant).

## Spec Excerpt

`verdict-audit.md` §13.4 (verbatim, design review C-1):

> The payload gains `verdict_id: string`, following the pattern the schema
> already uses — `verdict_corrected.original_verdict_id` and
> `reasoning_trace_generated.verdict_id`. The value is the id of the verdict the
> page actually rendered, threaded from the render, **not** re-derived by a
> second "latest event" lookup at write time. A second lookup would reintroduce
> precisely the race this closes.
>
> **The race it closes.** Without the id, a `verdict_corrected` event landing
> between the page's load and the reviewer's click means the attestation is
> recorded against whatever is current, with nothing recording what the reviewer
> saw. It could never afterwards be established — for or against — which verdict
> was attested. For an attestation this product frames as carrying legal weight,
> that is the defect, not an edge case.
>
> Where the rendered verdict's id no longer matches the latest verdict-bearing
> event at write time, the write is refused and the reviewer is told the verdict
> changed while they were reading, rather than silently attesting to the new one.

**Verified current state (BC-001):** the payload union is at
`src/store/types.ts:29`; both sign-off handlers build it in
`src/components/RegisterDetail.tsx` (`handleApprove`, `handleRequestCorrection`),
each already guarded by the synchronous `useRef` in-flight flag.

## Test cases

TC-R3-RD-3-01 (the latest verdict is the one shown after a correction),
TC-R3-RD-3-02 (the event names the rendered verdict), TC-R3-RD-3-03 (a verdict
changing under the reviewer refuses the write), plus TC-R3-RD-1-02, -2-02,
-5-01 above.

## Framework Reference

- The refusal must re-read the trail at write time to detect the change, then
  compare against the **rendered** id. That is not the same as deriving the id
  to write — read to detect, write what was shown.
- Testing Library: `findBy*` for the async load; scope queries (§11.1).

## TDD Approach

User-facing chunk — TDD-1 applies. Acceptance test first: TC-R3-RD-3-02, since
it is the assertion the schema change exists to make possible.

1. Write TC-R3-RD-3-02 and -3-03 failing. Confirm Red, record the SHA.
2. Add the schema field; thread it; add the refusal.
3. **Realistic-fixture variant (TDD-3, concurrency):** the correction landing
   *between* load and click — the shape a happy-path test never produces, and
   the one the requirement exists for.
4. Close the three coverage gaps.
5. Confirm Green.

## Review Criteria (self-review loop)

- **What does a reviewer see when the write is refused, and does it tell them
  what to do?** A refusal that reads as a failure is worse than useless on a
  page where someone is trying to discharge an obligation.
- **Beck:** would TC-R3-RD-3-02 pass vacuously if `verdict_id` were written as
  `''` or as a re-derived value? Assert the *rendered* verdict's id.
- **Leveson:** is any path still able to attest against a verdict the reviewer
  did not see?
- **R3-NF-2:** the in-flight guard is unchanged and still required; the refusal
  must not introduce a second write path.
- **No absence-by-absence** — the standing lesson of this round, now four
  chunks running.

### Known patterns to avoid (build checks)

- **BC-001** — cite `file:line` for every claim about existing code.
- **A test whose own setup is the only reason its claim holds** (P8-C05's
  null fixture; P8-C07's default parameter). Check the fixture before trusting
  the green.
